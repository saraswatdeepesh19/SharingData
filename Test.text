server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: C:\promtail\positions.yaml

clients:
  - url: http://<LOKI-HOST>:3100/loki/api/v1/push

scrape_configs:
  - job_name: windows-services-logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: windows-services
          __path__: D:/Digital/Services/*/logs/app.log
    pipeline_stages:
      - match:
          selector: '{job="windows-services"}'
          stages:
            - regex:
                expression: '.*Services\\(?P<service>[^\\]+)\\logs\\app.log'
            - labels:
                service
